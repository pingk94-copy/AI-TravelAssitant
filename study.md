# 智能旅行助手项目学习文档

## 1. 文档目标

这份文档不是简历润色稿，而是面向你自己落地项目时使用的“项目拆解设计文档”。

目标有三个：

1. 分析两份简历项目到底在做什么，本质能力点是什么。
2. 帮你确定一个适合个人实现、可演示、可写进简历的智能旅行助手方案。
3. 给出可以直接照着做的系统设计，包括前端、后端、数据库、Agent、接口和开发顺序。

建议你把最终项目定位为：

- 一个可运行的 `Vue 3 + FastAPI` 全栈 Web 项目
- 同时具备一定 AI 工程化特点：工具调用、多 Agent、流式输出、异步任务、鉴权、容错
- 重点不是“功能堆得多”，而是“架构清晰、链路完整、能稳定演示”

---

## 2. 两个参考项目的本质分析

## 项目 A：全栈 AI 行程规划系统

关键词：

- React/TypeScript/Vite/Tailwind
- FastAPI
- LangChain
- SQLite
- SSE
- JWT
- 多 Agent 协作
- Function Calling
- 异步任务

这个项目更像“产品化系统”。

它强调的是：

1. 有完整前后端
2. 有用户登录体系
3. 有 AI 行程规划主链路
4. 有实时交互体验
5. 有容错和工程化能力

它的核心卖点不是单纯用了大模型，而是把大模型放进了一个完整 Web 产品里。

简化后，它的主流程大概是：

1. 用户输入旅行需求
2. 后端启动 Planner Agent
3. Planner 拆分子任务给多个 Search Agent
4. Search Agent 调用天气、景点、酒店、地图等工具
5. 后端汇总结果生成最终 itinerary
6. 前端展示结构化行程结果

它最值得学习的点：

- 多 Agent 拆分思路
- 工具调用的 Schema 设计
- 长耗时任务与前端解耦
- SSE 流式交互
- JSON 解析和 fallback 容错
- 匿名用户到注册用户的数据迁移

## 项目 B：去哪 AI 智能出行服务助手

关键词：

- Python / FastAPI
- LangChain 1.2.15
- LangGraph 1.1.6
- SQLite
- Gradio
- Tavily
- 高德地图 API
- 多节点工作流
- 敏感操作确认
- pytest

这个项目更像“AI 工作流系统”。

它强调的是：

1. 多能力工具接入
2. LangGraph 工作流编排
3. 子助手路由
4. 人工确认中断机制
5. 测试和可验证性

它比项目 A 更偏后端和 Agent 编排，前端演示相对轻，重点在 AI 决策流程本身。

它的主流程大概是：

1. 用户提出旅行相关问题
2. 主助手判断意图
3. 根据意图路由到航班、酒店、租车、景点、旅行推荐等节点
4. 子节点调用工具或数据库
5. 涉及高风险操作时暂停并请求用户确认
6. 汇总结果返回

它最值得学习的点：

- LangGraph 的节点式流程控制
- 主助手 + 专项助手的分工
- 工具安全分级
- 人工确认机制
- 面向测试的后端设计

---

## 3. 两个项目的共性与差异

## 共性

这两个项目虽然包装方式不同，但本质能力是一致的：

1. 都是“自然语言输入 -> AI 理解 -> 工具调用 -> 结构化输出”
2. 都依赖外部 API，而不是只靠模型“编”
3. 都把旅行场景拆成多个垂直能力：天气、地点、路线、酒店、推荐
4. 都需要后端统一编排 AI 和业务能力
5. 都适合做简历项目，因为兼顾了 AI、后端、工程化和产品化

## 差异

项目 A 更偏：

- 全栈产品
- 用户体验
- 异步任务
- 实时流式输出
- 工程可靠性

项目 B 更偏：

- Agent 工作流
- 路由与状态流转
- 多节点编排
- 工具安全
- 测试体系

## 对你来说应该怎么取舍

最适合你的路线不是二选一，而是融合：

1. 用项目 A 的产品骨架
2. 用项目 B 的 Agent 编排思想
3. 前端改成 `Vue 3`

也就是：

- 前端做成真实 Web 应用
- 后端做成可扩展 AI 编排系统
- AI 部分先实现“能跑通”，再逐步增强为多 Agent

---

## 4. 推荐你的项目定位

推荐项目名：

`AI 智能旅行助手`

推荐技术栈：

- 前端：`Vue 3 + TypeScript + Vite + Pinia + Vue Router + Tailwind CSS`
- 后端：`FastAPI + Pydantic + SQLAlchemy`
- AI 编排：`LangChain`
- 可选工作流增强：`LangGraph`
- 数据库：`SQLite` 起步，后续可切 `MySQL`
- 鉴权：`JWT + bcrypt`
- 实时输出：`SSE`
- 并发：`asyncio` 或线程池
- 测试：`pytest`
- 第三方能力：`高德地图 API`、天气 API、酒店/POI 搜索 API、联网搜索

你的项目目标不建议一开始就做“航班预订、酒店真实下单、改签”这种复杂业务。

更合理的 MVP 是：

1. 用户输入自然语言旅行需求
2. 系统自动抽取出发地、目的地、时间、偏好、预算
3. 查询天气、景点、地点、路线等信息
4. 生成 1 到 5 天旅行行程
5. 前端实时展示 AI 生成过程或回答过程
6. 用户可登录并保存历史行程

这样复杂度适中，而且很好讲。

---

## 5. 建议的系统架构

推荐采用四层结构：

1. 前端展示层
2. 后端接口层
3. AI 编排层
4. 数据与外部服务层

## 5.1 前端展示层

前端职责：

- 用户登录注册
- 对话输入与展示
- 行程规划表单
- 行程结果展示
- 历史记录页
- 收藏页
- 流式消息展示
- 任务进度展示

推荐页面：

1. 登录/注册页
2. 首页
3. AI 对话页
4. 行程规划页
5. 我的行程页
6. 行程详情页

## 5.2 后端接口层

后端职责：

- 接收前端请求
- 做鉴权和参数校验
- 调度 AI 编排逻辑
- 调用外部 API
- 管理异步任务
- 管理对话和行程数据

## 5.3 AI 编排层

这是项目核心。

职责：

- 解析用户意图
- 决定调用哪些工具
- 并发查询多个旅行信息源
- 汇总结果
- 输出结构化 itinerary

## 5.4 数据与外部服务层

包括：

- SQLite / MySQL
- 高德地图 API
- 天气 API
- 联网搜索工具
- 酒店/景点数据源

---

## 6. 推荐的模块拆分

建议后端按下面方式拆模块：

### 6.1 用户模块

功能：

- 注册
- 登录
- 匿名用户生成
- JWT 签发与校验
- 匿名数据迁移到账户

### 6.2 对话模块

功能：

- 创建对话
- 保存用户消息
- 保存 AI 回复
- 获取聊天历史
- SSE 流式输出

### 6.3 行程规划模块

功能：

- 接收自然语言需求
- 生成规划任务
- 查询任务状态
- 获取规划结果

### 6.4 工具调用模块

功能：

- 封装高德 API
- 封装天气查询
- 封装 POI 搜索
- 封装酒店推荐
- 封装路线规划
- 统一处理 API 异常和超时

### 6.5 Agent 编排模块

功能：

- Planner Agent
- Search Agent
- 工具路由
- 输出校验
- fallback 兜底

### 6.6 数据持久化模块

功能：

- 用户表
- 行程表
- 对话表
- 收藏表
- 任务表

---

## 7. Agent 架构怎么设计

你可以直接采用“3 搜索 Agent + 1 Planner Agent”的思路，但建议你按可落地方式实现。

## 7.1 推荐版本

### Agent 1：Planner Agent

职责：

- 理解用户旅行需求
- 拆解任务
- 决定需要哪些查询
- 汇总多个子结果
- 生成最终结构化行程

输入示例：

- 出发地
- 目的地
- 出行日期
- 天数
- 预算
- 偏好
- 同行人群

输出示例：

- 每日行程安排
- 景点建议
- 路线建议
- 餐饮建议
- 注意事项

### Agent 2：Weather Search Agent

职责：

- 查询目的地天气
- 结合天气输出出行建议

### Agent 3：POI Search Agent

职责：

- 查询景点
- 查询热门区域
- 查询附近推荐

### Agent 4：Hotel/Route Search Agent

职责：

- 查询住宿区域建议
- 查询路线、通勤方式、距离和耗时

## 7.2 为什么这样分

因为这三类信息最容易形成完整行程：

1. 天气决定是否适合户外
2. 景点决定去哪玩
3. 酒店和路线决定住哪里、怎么走

这是一个足够简洁、又能体现多 Agent 的划分方式。

## 7.3 调度方式

推荐流程：

1. Planner 解析用户输入
2. Planner 生成查询任务列表
3. 后端并发执行 3 个 Search Agent
4. 收集结果
5. Planner 汇总并输出行程

实现方式可以不必一开始就写成特别复杂的“Agent 套 Agent”。

你可以分两阶段：

### 阶段 1

先做“伪多 Agent”：

- Planner 先输出要查什么
- 后端代码按类别调用不同工具函数
- 最后再交给 Planner 汇总

### 阶段 2

再升级为更像真正多 Agent 的结构：

- 每个 Search Agent 各自有 prompt
- 每个 Agent 可自主选工具
- 统一汇总结果

这样更适合个人项目推进。

---

## 8. 数据库怎么设计

建议先用 `SQLite`，因为本地开发最轻量。

核心表不要太多，保持简洁。

## 8.1 users 表

作用：

- 存储用户账号信息

建议字段：

- `id`
- `username`
- `email`
- `password_hash`
- `is_guest`
- `guest_uid`
- `created_at`
- `updated_at`

说明：

- 注册用户用 `username/email/password_hash`
- 匿名用户用 `guest_uid`
- `is_guest` 用来区分游客和正式账号

## 8.2 trips 表

作用：

- 存储一次行程规划结果

建议字段：

- `id`
- `user_id`
- `title`
- `origin`
- `destination`
- `start_date`
- `end_date`
- `days`
- `budget`
- `preferences`
- `status`
- `result_json`
- `created_at`

说明：

- `result_json` 用来保存最终结构化 itinerary
- `status` 可取 `pending / running / success / failed`

## 8.3 chat_sessions 表

作用：

- 存储会话

建议字段：

- `id`
- `user_id`
- `title`
- `created_at`

## 8.4 chat_messages 表

作用：

- 存储消息内容

建议字段：

- `id`
- `session_id`
- `role`
- `content`
- `created_at`

说明：

- `role` 可取 `user / assistant / system`

## 8.5 tasks 表

作用：

- 存储异步任务状态

建议字段：

- `id`
- `user_id`
- `task_type`
- `status`
- `input_json`
- `output_json`
- `error_message`
- `created_at`
- `updated_at`

说明：

- 用来支持“提交任务后轮询结果”

## 8.6 favorites 表

作用：

- 收藏行程或景点

建议字段：

- `id`
- `user_id`
- `favorite_type`
- `target_id`
- `created_at`

---

## 9. 接口怎么设计

你的接口设计应该体现三条主链路：

1. 用户认证链路
2. AI 对话链路
3. 行程规划链路

## 9.1 认证相关接口

### `POST /api/auth/register`

作用：

- 用户注册

请求体示例：

```json
{
  "username": "tom",
  "email": "tom@example.com",
  "password": "123456"
}
```

返回：

- 注册成功信息
- 可选直接返回 token

### `POST /api/auth/login`

作用：

- 用户登录并获取 JWT

返回：

```json
{
  "access_token": "xxx",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "username": "tom"
  }
}
```

### `POST /api/auth/guest`

作用：

- 创建匿名用户身份

用途：

- 用户没注册也能先体验

### `POST /api/auth/upgrade-guest`

作用：

- 把游客数据迁移到正式账号

这是一个很好的简历亮点。

## 9.2 AI 对话接口

### `POST /api/chat/session`

作用：

- 创建新会话

### `GET /api/chat/session/{session_id}`

作用：

- 获取消息历史

### `GET /api/chat/stream`

作用：

- SSE 流式返回 AI 对话内容

前端传入：

- `session_id`
- `message`

返回流式事件：

- token 片段
- 工具调用状态
- 最终结束标记

## 9.3 行程规划接口

### `POST /api/trips/plan`

作用：

- 提交一个规划任务

请求体示例：

```json
{
  "origin": "上海",
  "destination": "杭州",
  "start_date": "2026-06-01",
  "days": 3,
  "budget": "3000",
  "preferences": ["美食", "景点", "轻松"]
}
```

返回示例：

```json
{
  "task_id": "task_xxx",
  "status": "pending"
}
```

### `GET /api/tasks/{task_id}`

作用：

- 查询规划任务状态

返回：

- `pending`
- `running`
- `success`
- `failed`

### `GET /api/trips/{trip_id}`

作用：

- 获取某次行程详情

### `GET /api/trips`

作用：

- 获取我的历史行程列表

## 9.4 收藏接口

### `POST /api/favorites`

作用：

- 收藏行程或景点

### `GET /api/favorites`

作用：

- 获取收藏列表

---

## 10. SSE、异步任务、普通接口分别适合什么

这个项目里常见的三种交互方式：

## 10.1 普通同步接口

适合：

- 登录
- 注册
- 获取列表
- 获取详情

特点：

- 请求后很快返回结果

## 10.2 SSE

适合：

- AI 聊天流式回复
- 长回答逐段返回
- 工具调用过程可视化

特点：

- 后端不断推送
- 前端体验更好

## 10.3 异步任务 + 轮询

适合：

- 行程规划这类耗时较长的操作
- 涉及多个 API 并发查询的任务

特点：

- `POST` 先返回 `task_id`
- 前端轮询任务状态
- 后端完成后再取结果

为什么不建议一开始就把“行程规划”也写成 SSE？

因为对初学者来说：

1. SSE 更适合流式文本
2. 复杂规划通常要经历多个步骤，任务式更好管理状态
3. 异步任务模型更容易做失败重试和结果存储

所以推荐：

- AI 问答：SSE
- 行程规划：异步任务

---

## 11. 工具调用怎么设计

你要把外部能力封装成“模型可调用工具”。

推荐从下面几类开始：

## 11.1 地点搜索工具

功能：

- 根据关键词查地点
- 输入“西湖”“杭州东站”这类词返回位置结果

## 11.2 天气查询工具

功能：

- 查询某城市或区域天气

## 11.3 周边景点工具

功能：

- 查询景点
- 查询热门 POI

## 11.4 路线规划工具

功能：

- 查询 A 到 B 的驾车/步行/公交路线

## 11.5 酒店区域建议工具

功能：

- 不一定非要接真实酒店预订 API
- 可以先做“推荐住宿区域 + 理由”

这对个人项目更现实。

## 11.6 联网搜索工具

功能：

- 补充开放式旅游信息

注意：

- 不是所有问题都要联网
- 要有限制，避免无限制搜索导致输出混乱

---

## 12. 输出格式与容错怎么做

这是智能项目里非常关键的一点。

大模型最常见的问题不是“不会回答”，而是“输出不稳定”。

你可以借鉴第一份简历里的三级容错链。

## 12.1 第一层：Prompt 强约束

要求模型输出固定 JSON 结构，例如：

```json
{
  "summary": "",
  "days": [
    {
      "day": 1,
      "theme": "",
      "schedule": []
    }
  ],
  "tips": []
}
```

## 12.2 第二层：JSON 提取修复

后端做：

- 提取 markdown 代码块中的 JSON
- 去除多余文本
- 尝试修复尾逗号、引号等格式问题

## 12.3 第三层：模板兜底

如果模型始终没返回合法 JSON：

- 用已有工具结果拼装一个基础模板
- 至少保证前端可以显示

这会显著提升你的项目稳定性。

---

## 13. 并发和性能怎么做

你的项目里，最值得写进简历的性能点是“并发查询”。

## 推荐方案

Planner 确定需要查询：

- 天气
- 景点
- 路线

后端并发执行这三类任务。

实现方式：

- 简单版：`asyncio.gather`
- 兼容阻塞版：`ThreadPoolExecutor`

为什么需要并发：

因为多个外部 API 串行执行会明显拖慢整体规划速度。

如果串行：

- 天气 2 秒
- 景点 3 秒
- 路线 2 秒
- 汇总 3 秒

总计可能要 10 秒以上。

如果并发：

- 多数查询可压缩到 3 到 5 秒，再加汇总

这就是一个不错的简历亮点。

---

## 14. 安全与风险控制怎么做

即使这是个人项目，也要体现安全意识。

## 14.1 JWT 鉴权

保护：

- 历史行程
- 聊天记录
- 收藏数据

## 14.2 敏感操作确认

如果以后你扩展到：

- 改签
- 预订
- 下单

不要让 AI 自动执行。

应该设计成：

1. AI 给出拟执行操作
2. 后端把操作标记为 `requires_confirmation`
3. 前端弹窗让用户确认
4. 用户确认后再真正执行

即使你当前版本不实现真实下单，也可以在文档和代码结构里预留这个机制。

## 14.3 工具白名单

只允许模型调用你注册过的工具，不允许任意自由执行。

## 14.4 API 超时与失败处理

每个外部工具都应有：

- 超时设置
- 错误捕获
- 默认降级结果

---

## 15. 前端应该怎么设计

既然你用 `Vue 3`，前端建议简洁但完整。

## 推荐页面结构

### 首页

展示：

- 产品介绍
- 输入框
- 入口按钮

### AI 对话页

展示：

- 左侧历史会话
- 右侧消息区
- 底部输入框
- SSE 流式消息

### 行程规划页

展示：

- 出发地
- 目的地
- 日期
- 天数
- 预算
- 偏好标签

提交后展示：

- 规划中状态
- 最终生成结果

### 行程详情页

展示：

- 行程摘要
- 每日安排
- 路线建议
- 天气建议
- 花费建议

## 前端状态管理建议

使用 `Pinia` 管理：

- 当前用户
- token
- 当前会话
- 当前任务状态
- 当前行程结果

---

## 16. 推荐的项目目录结构

## 前端

```text
frontend/
  src/
    api/
    components/
    views/
    stores/
    router/
    types/
    utils/
```

## 后端

```text
backend/
  app/
    api/
    core/
    models/
    schemas/
    services/
    agents/
    tools/
    db/
    utils/
    main.py
```

你可以这样理解：

- `api/` 放路由
- `models/` 放 SQLAlchemy 表模型
- `schemas/` 放 Pydantic 请求响应模型
- `services/` 放业务逻辑
- `agents/` 放 Planner 和 Search Agent
- `tools/` 放高德、天气、路线等工具封装

---

## 17. 推荐你的开发顺序

这是最重要的一节。很多人项目做不出来，不是因为不会，而是顺序错了。

## 第 1 阶段：先搭全栈骨架

完成：

- Vue 3 前端初始化
- FastAPI 后端初始化
- 数据库连接
- 基础路由
- 跨域配置

目标：

- 前后端能跑通

## 第 2 阶段：做用户系统

完成：

- 注册
- 登录
- JWT 鉴权
- 用户信息接口

目标：

- 有完整登录链路

## 第 3 阶段：做聊天基础功能

完成：

- 聊天页面
- 会话表和消息表
- SSE 流式输出

目标：

- 能像一个 AI 助手一样基本对话

## 第 4 阶段：做旅行工具封装

完成：

- 高德地点搜索
- 天气查询
- 路线查询
- 景点查询

目标：

- 后端具备真实旅行数据获取能力

## 第 5 阶段：做行程规划 MVP

完成：

- 接收结构化旅行参数
- 调用工具
- 输出固定 JSON 行程

目标：

- 能生成基础 itinerary

## 第 6 阶段：升级到多 Agent

完成：

- Planner Agent
- 3 个 Search Agent
- 并发执行
- 汇总输出

目标：

- 项目具备 AI 工程化亮点

## 第 7 阶段：做异步任务和容错

完成：

- `POST /plan` 返回 `task_id`
- 任务状态表
- 轮询任务结果
- JSON 容错链

目标：

- 稳定性和体验更像真实产品

## 第 8 阶段：补测试和文档

完成：

- pytest
- 接口文档
- 部署说明
- 简历项目描述

目标：

- 项目可验收、可展示、可讲解

---

## 18. 你最应该做出来的 MVP

如果你时间有限，不要追求“大而全”。

最小可行版本建议只做这些：

1. 注册登录
2. AI 对话页
3. 行程规划页
4. 天气/景点/路线三个工具
5. Planner + 3 Search Agent
6. 异步任务
7. 行程结果展示

只要这条链路完整，你的项目就已经足够能打。

---

## 19. 这个项目的简历亮点应该怎么理解

你做这个项目，不是为了炫技，而是为了输出下面这些能力信号：

1. 你会做 AI 应用，而不是只会调接口
2. 你能把模型能力和真实业务结合
3. 你会做前后端联调
4. 你具备一定系统设计能力
5. 你关注稳定性、容错和用户体验

面试官真正想看到的，不是“你用了多少名词”，而是：

- 为什么要多 Agent
- 为什么要异步任务
- 为什么要用 SSE
- 为什么要做 JSON 容错
- 为什么要分用户表、任务表、会话表

只要你自己能把这些理由讲清楚，这个项目就有价值。

---

## 20. 最后给你的落地建议

不要一开始就追求“和简历里写得一样复杂”。

正确顺序是：

1. 先做一个能跑通的单 Agent 版本
2. 再抽象成 Planner + Search Agent
3. 再补异步任务、SSE、JWT、收藏、历史记录
4. 最后再考虑 LangGraph、人工确认、测试完善

也就是说，项目建设顺序应该是：

`先完整，再复杂；先可运行，再可包装`

---

## 21. 推荐你下一步立刻做什么

如果你准备正式开始这个项目，建议下一步按这个清单执行：

1. 建立 `frontend` 和 `backend` 两个目录
2. 前端初始化 `Vue 3 + TypeScript + Vite + Tailwind + Pinia + Router`
3. 后端初始化 `FastAPI + SQLAlchemy + Pydantic`
4. 先把用户注册登录打通
5. 再做一个最简单的“输入需求 -> 返回固定 itinerary JSON”的假数据版本
6. 然后逐步接入真实工具和 AI 编排

这会比你上来直接冲多 Agent 成功率高很多。

---

## 22. 一句话总结

这两个参考项目本质上分别代表了两种能力方向：

- 一个偏“全栈产品化 AI 系统”
- 一个偏“多 Agent 工作流系统”

你最适合做的版本，是把两者融合成一个 `Vue 3 + FastAPI` 的智能旅行助手：前端完整、后端清晰、AI 能力可演示、技术点足够硬，但整体复杂度仍然在个人项目可控范围内。
